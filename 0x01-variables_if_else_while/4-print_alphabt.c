#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Succss)
*/
int main(void)
{

	char w = 'a';

	for ( ; w <= 'z' ; w++)
	{
		if (w != 'e' && w != 'q')
		putchar (-);
	}
	putchar ('\n');
return (0);

}
